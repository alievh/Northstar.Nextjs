import { FC } from "react";

interface checkoutProps {}

const checkout: FC<checkoutProps> = ({}) => {
  return (
      <main>
        <h1>CheckoutPage</h1>
      </main>
  );
};

export default checkout;
